UNDO POINTS

  pre-palette   06e2399   before black/orange
  pre-pwa       cb85604   before the installable app
  pre-icons     ac061b6   before the card icons
  pre-polish    f258e38   before entrance animation + stat strip

To undo the polish only:
  git revert --no-edit f258e38..cc8829b && git push

To go back wholesale to before the polish:
  git reset --hard pre-polish && git push --force

To undo the icons only:
  git revert --no-edit ac061b6..2af99b3 && git push

To undo the PWA only:
  git revert --no-edit cb85604..b0e71cb && git push
